<!doctype html>
<html lang="en">
  <head>
    <script>
      function signup(){
          location.href ="/signup2";
      }; 
    </script>
    <script src="https://code.jquery.com/jquery-3.3.1.min.js"></script>
    <script>
        $(function(){
            $("#success").hide();
            $("#danger").hide(); 
            $("#signup").attr("disabled",true)
            $("input").keyup(function(){
                var pass1 = $("#input_password").val();
                var pass2 = $("#input_confirm").val();
                if(pass1 && pass2){
                        if(pass1 ==pass2){
                        $("#success").show();
                        $("#danger").hide(); 
                        $("#signup").attr("disabled", false);
                    }else{
                        $("#success").hide();
                        $("#danger").show();
                        $("#signup").attr("disabled", true);
                    }
                }else{
                    $("#success").hide();
                    $("#danger").hide();
                    $("#signup").attr("disabled",true)
                }
            })   
        })
    </script>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="Mark Otto, Jacob Thornton, and Bootstrap contributors">
    <meta name="generator" content="Hugo 0.84.0">
    <title>Signin Template · Bootstrap v5.0</title>

    <link rel="canonical" href="https://getbootstrap.com/docs/5.0/examples/sign-in/">

    

    <!-- Bootstrap core CSS -->
<link href="./css/bootstrap.min.css" rel="stylesheet">

    <style>
      .bd-placeholder-img {
        font-size: 1.125rem;
        text-anchor: middle;
        -webkit-user-select: none;
        -moz-user-select: none;
        user-select: none;
      }

      @media (min-width: 768px) {
        .bd-placeholder-img-lg {
          font-size: 3.5rem;
        }
      }
    </style>

    
    <!-- Custom styles for this template -->
    <link href="./css/signin.css" rel="stylesheet">
  </head>
  <body class="text-center">
    
<main class="form-signin">
  <form action = "/signup2" method = "POST">
    <img class="mb-4" src="./css/image/bootstrap-logo.svg" alt="" width="72" height="57">
    <h1 class="h3 mb-3 fw-normal">Please sign in</h1>

    <div class="form-floating">
      <input type="text" class="form-control" id="input_id" name="post_id" placeholder="name@example.com">
      <label for="floatingInput">Email address</label>
    </div>
    <div class="form-floating">
      <input type="password" class="form-control" id="input_password" name="password" placeholder="Password">
      <label for="floatingPassword">Password</label>
    </div>
    <div class="form-floating">
        <input type="password" class="form-control" id="input_confirm" placeholder="Conform_Password">
        <label for="floatingConformPassword">Confirm Password</label>
      </div>
    <div class="alert alert-success" id="success">
        비밀번호가 일치합니다.
      </div>
      <div class="alert alert-danger" id="danger">
        비밀번호가 다릅니다.
      </div>
    <div class="form-floating">
        <input type="password" class="form-control" id="input_name" name="name" placeholder="Name">
        <label for="floatingName">Name</label>
    </div>

    <div class="checkbox mb-3">
      <label>
        <input type="checkbox" value="remember-me"> Remember me
      </label>
    </div>
    <button class="w-100 btn btn-lg btn-primary" type="submit">Sign in</button>
    <button style="margin-top:10px;" id = "signup" class="w-100 btn btn-lg btn-primary" type="submit" onclick="signup()">Sign up</button>
  </form>
    <p class="mt-5 mb-3 text-muted">&copy; 2017–2021</p>
  
</main>


    
  </body>
</html>
